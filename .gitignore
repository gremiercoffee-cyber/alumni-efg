node_modules/
.env
.expo/
dist/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
.DS_Store

# expo build artifacts (these were tracked by mistake)
.tmp-export/
.expo-check/
web-build/
migration/out/
__pycache__/
*.pyc
android/
ios/

# Built APKs. They belong in Releases, not in git -- a 61MB blob per build
# would bloat every clone forever.
*.apk
*.aab

# Local gradle build output
build.log
build.err

# Firebase config for the Android build. Not secret -- it ships inside every
# APK -- but the repository is public and there is no reason to publish the
# project identifiers. A CI build needs it supplied as a secret.
google-services.json
